# Changelog

눈에 띄는 변경을 기록한다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다. 프로토타입 단계라 0.x 로 둔다.

버전은 매 변경마다 올리지 않는다. 미발행 변경은 Unreleased 에 쌓고, 작업이 마무리되거나 충분히 쌓였을 때 사용자가 정한 시점에 한 번에 올린다.

버전을 올릴 때 `app/package.json` 과 `app/app.json` 을 함께 올린다. app.json 의 버전은 프리빌드 때만 네이티브에 반영되므로, 로컬 `app/ios/` (커밋되지 않는 프리빌드 산출물)가 이미 있으면 그 안의 버전 필드(Info.plist `CFBundleShortVersionString`, `MARKETING_VERSION`)도 손으로 맞추거나 프리빌드를 다시 돌린다. 안 맞추면 기기에 낡은 버전 라벨로 설치된다.

## [0.5.1] - 2026-08-21

### Fixed

- 탭 바 활성 표시를 버튼 모양(알약)에서 표준 색 강조로 되돌렸다
- 탭 바 아래 공백 밴드를 홈 인디케이터 영역만큼으로 줄이고, 바를 불투명 단색으로 바꿔 뒤 내용 비침을 없앴다

## [0.5.0] - 2026-08-21

### Added

- 기록이 그때의 지점을 제 것으로 갖고, 기록 상세 지도에 지점 핀이 그려진다

### Changed

- 기록 상세 전체가 스크롤된다. 구간 목록이 가려지지 않는다
- 기록 상세의 지도가 달리기 화면과 같은 비율(화면 높이 몫)로 줄었다
- 하단 바 밑 안전영역을 바 색으로 채우고, 활성 탭을 기존 버튼 어휘(테마 색 바탕 + 흰 글자)로 통일했다

## [0.4.0] - 2026-08-21

### Added

- 하단 바(달리기 · 기록 · 코스)로 화면을 오간다. 오른쪽 위 버튼 두 개를 걷었다
- 달리기 종료 직후, 이름 없는 코스면 저장 유도 다이얼로그가 뜬다. 요약을 보여주고 이름을 받아 그 자리에서 저장한다. 화면은 옮기지 않고 마지막 기록이 유지된다

### Changed

- 코스·기록 화면의 닫기 버튼을 걷었다. 하단 바가 그 역할을 한다

### Fixed

- 제자리 정지 잡음이 만드는 사람 밖 페이스(세 자리 분)를 화면에 내지 않는다

## [0.3.0] - 2026-08-21

### Added

- 모든 달리기가 코스 유무와 무관하게 기록으로 남는다. 기록마다 표시용 경로(결손 조각 유지, 400점 상한)가 함께 남는다
- 기록 화면: 최근순 목록(날짜·거리·시간·평균 페이스·코스명)과 상세(경로 지도·전체 통계·구간 페이스). 달리기 화면의 «기록» 버튼으로 연다
- 기록 보관 상한 50건. 넘치면 오래된 것부터 밀려난다

## [0.2.0] - 2026-08-21

### Added

- 저장된 코스가 자기 기록을 항상 갖는다. 이름을 붙여 저장하면 그 전에 달린 기록도 그 코스로 이관된다
- 마지막 달리기 칸에서도 «기록 보기»가 그 코스의 기록을 찾는다

### Changed

- 코스를 지워도 달리기 기록은 남는다. 달리기는 일어난 사실이고 코스는 그것을 묶는 이름이다

## [0.1.0] - 2026-08-21

### Added

- 메인 계기판이 전체 평균 페이스를 보여준다. 현재(창) 페이스는 보조다
- 구간이 지점을 지나는 순간(도달·달리며 표시·종료) 닫힌다. 기록 달리기에도 구간 페이스가 남는다
- 진행 중 구간이 지점 목록 자리에서 실시간 갱신된다. 종료하면 그 화면이 그대로 요약이 된다
- 달리기 전 화면에 마지막 달리기 카드와 코스 기준 경로(점선)가 보인다
- 코스 화면의 «기록 보기»가 코스별 전체 통계와 구간별 페이스를 펼친다
- 커버리지 게이트: 도메인·애플리케이션 층 라인 80% 미만이면 게이트가 실패한다

### Fixed

- 기기 시험이 진단 세션을 상한(30분)까지 남겨 시험 뒤에도 음성이 계속 나오던 문제
- 달리기 전 화면의 다음 지점이 «모두 지남»으로 보이던 표기

### Changed

- 기기 시험과 설치가 같은 와이파이에서 케이블 없이 돈다
- 짧은 화면(아이폰 13 미니)에서 지도에 화면 높이의 몫을 보장한다
- 구현하지 않은 게이미피케이션 예비 기록을 문서에서 걷었다
