# 치어러너 작업 규칙

이 파일은 세션마다 주입되므로, **잊으면 되돌리기 비싼 것만** 둔다. 설명은 문서에 있고 여기서는 가리킨다.

## 계약이 코드보다 앞선다

작성자는 코드를 읽지 않는다. 구현이 [docs/DOMAIN.md](docs/DOMAIN.md) 의 상수·판정식·불변식과 다르면 구현이 틀린 것으로 본다. 편의를 위해 계약을 바꾸지 않는다. 실제로 한 번 그렇게 갔다가 문서를 확인하고 코드를 되돌렸다.

계약을 바꿔야 한다면 그것이 결정이므로 [docs/adr/](docs/adr/) 에 기록하고 사람의 승인을 받는다.

## 문서마다 담는 것이 다르다

| 문서 | 담는 것 | 담지 않는 것 |
|---|---|---|
| `DOMAIN.md` | 용어·값·불변식·상수·정책 | 프레임워크 이름 |
| `ARCHITECTURE.md` | **현재** 구조와 그렇게 된 이유 | 예비 계획. 앞으로 올 수 있는 것 |
| `VERIFICATION.md` | 확인 절차 | 무엇이 맞아야 하는지 |
| `MVP.md` | 범위와 순서 | 상수·명세 |
| `adr/` | 결정과 근거 | 변경 이력 |

규약 정본은 [docs/CONVENTIONS.md](docs/CONVENTIONS.md).

## 측정 기록을 빼먹지 않는다

마일스톤에 닿을 때 [docs/MEASURE.md](docs/MEASURE.md) 에 한 줄 적는다. 마일스톤은 사람이 기기에서 확인할 수 있는 상태에 도달한 시점이고, PR 하나가 아니다.

부르지 않은 도구는 0 이 아니라 미측정으로 적는다.

## 확인은 층으로 나눈다

값이 맞는지와 화면이 온전한지는 다른 사실이다. 시험을 통과했다고 완료로 보고하지 않는다. 화면이 바뀌면 `npm run test:screen` 으로 그림을 만들어 눈으로 본다. 절차는 [docs/VERIFICATION.md](docs/VERIFICATION.md).

기기에서만 보이는 것은 두 가지다. 배경에서 소리가 끝까지 나는가, 무음 스위치에서 들리는가.

## 웹은 정적 페이지로만 돌아온다

도메인 규칙을 웹에 두지 않는다. 계약을 읽고 구현하는 것은 앱 하나다. 경계선과 다시 볼 조건은 [ADR 0009](docs/adr/0009-web-returns-as-static-page-only.md).

## 용어

화면·주석·문서가 같은 낱말을 쓴다. 「달리기」이고 「주행」이 아니다. 저장 단위는 「코스」이고 지도 위젯과 구분한다.

한국어 용어를 일괄 치환할 때 받침이 바뀌면 조사도 깨진다. 치환 → 조사 교정 → 활용형 손 검토 순서로 본다.
