// 알림음 포트. application 이 필요를 선언하고 infrastructure 가 채운다.
//
// 음성 포트와 따로 두는 이유는 판정이 다르기 때문이다. 음성은 «끝까지 재생됐는가» 를
// 봐야 해서 결과 콜백이 계약에 들어간다. 알림음은 0.3초짜리 한 소리라 끝까지 갔는지
// 물을 일이 없고, 그것을 물으려고 콜백을 두면 부르는 쪽이 쓰지 않는 분기를 갖는다.
//
// 소리로 무엇을 알리는지는 부르는 쪽이 정한다. 이 포트는 한 소리만 낸다.
// 소리를 여러 개 두면 어느 소리가 무슨 뜻인지 러너가 외워야 한다.

/**
 * @typedef {Object} CuePort
 * @property {() => void} play
 *   짧은 확인 소리를 낸다. 앞 소리가 아직 나는 중이면 처음으로 되돌려 다시 낸다.
 *   실패해도 던지지 않는다. 소리는 조작의 결과를 알리는 것이고, 조작 자체가 아니다
 */

export const CUE = { file: 'cue.wav' };
