// 연결 포트. application 이 필요를 선언하고 infrastructure 가 채운다.
//
// 왜 달리기 앱이 인터넷을 보는가. 지도 타일이 인터넷에서 온다. 지도가 안 그려지면
// 지점을 찍을 수 없고, 지점을 찍을 수 없으면 이 앱이 하는 일이 없다.
//
// 거리와 응원은 인터넷 없이도 돈다. 그래서 이것은 **시작 전 조건**이고 달리는 중 조건이
// 아니다. 달리는 중에 끊겼다고 멈추면 기록이 사라진다.
//
// 「연결됨」과 「인터넷에 닿음」은 다른 사실이다. 신호는 잡혀 있고 밖으로 못 나가는
// 상태가 실제로 있다. 그때 지도는 안 그려지므로 닿는 쪽을 본다.

/**
 * @typedef {Object} NetworkPort
 * @property {() => Promise<boolean>} isOnline
 *   인터넷에 닿는가. 판단할 수 없으면 참으로 본다. 알 수 없는 것을 끊긴 것으로 보면
 *   연결이 살아 있는데도 달리기를 막는다
 * @property {(fn:(online:boolean)=>void) => {remove:()=>void}} subscribe
 *   상태가 바뀔 때 부른다. 화면이 이것으로 배너를 지운다
 */
