// 색 정본. 화면은 값을 직접 적지 않고 여기서 가져온다.
//
// 한 자리에 모으는 이유는 같은 색이 여러 파일에 적혀 한쪽만 바뀌는 것을 막기 위해서다.
// 실제로 그렇게 됐다. 경로선·지점 원·버튼·안드로이드 알림 배너가 각각 같은 값을 적고 있었다.
//
// 계열은 블로그와 여행 앱이 쓰는 파스텔이다. 배경은 민트에서 라벤더를 지나 웜으로 가고,
// 포인트는 파랑 하나로 둔다.
//
// 버튼 바탕과 라벨의 대비를 값으로 적어 둔다. 달리는 중에 햇빛 아래서 보는 화면이라
// 대비가 모자라면 라벨이 회색으로 뭉개진다. 밝은 파랑(#17a1fa)은 흰 글자와 1.8:1 이라
// 버튼 바탕으로 쓰지 않고, 같은 계열의 진한 파랑을 쓴다. 밝은 쪽은 선과 강조에 남는다.

export const COLOR = {
  // 배경. 위에서 아래로 세 단
  canvas: ['#e4f1ea', '#e9e9f5', '#f6efe4'],

  // 글자
  ink: '#121212',
  inkSoft: '#6b7280',
  inkFaint: '#9ca3af',

  // 버튼 바탕. 괄호 안은 흰 라벨과의 대비
  run: '#1270b0',      // 달리기 (5.3:1)
  stop: '#121212',     // 종료 (18.9:1)
  mark: '#629126',     // 여기 표시 (3.8:1)
  onDark: '#ffffff',

  // 지도
  path: '#17a1fa',            // 달린 경로
  spot: '#ff8a9d',            // 아직 안 지난 지점
  spotFill: 'rgba(255,138,157,0.20)',
  spotDone: '#9ca3af',        // 지난 지점
  spotDoneFill: 'rgba(156,163,175,0.15)',
  mapWait: '#e7ecea',

  // 표면. 파스텔 배경이 카드 뒤로 배어 나오게 반투명으로 둔다
  card: 'rgba(255,255,255,0.62)',
  cardLine: 'rgba(255,255,255,0.70)',
  divider: 'rgba(18,18,18,0.07)',

  // 상태
  warn: '#9a6700',
  ok: '#335803',
  danger: '#b3261e'
};
